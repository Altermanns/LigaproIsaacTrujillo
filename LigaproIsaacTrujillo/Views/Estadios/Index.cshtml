@model IEnumerable<LigaproIsaacTrujillo.Models.Estadio>

@{
    ViewData["Title"] = "Estadios";
}

<div class="container mt-5">
    <div class="row mb-4">
        <div class="col">
            <h2 class="text-center">Lista de Estadios</h2>
        </div>
        <div class="col text-end">
            <a class="btn btn-primary" asp-action="Create">
                <i class="bi bi-plus-circle"></i> Agregar Estadio
            </a>
        </div>
    </div>

    <table class="table table-striped table-hover table-bordered align-middle">
        <thead class="table-dark">
            <tr>
                <th>Nombre</th>
                <th>Dirección</th>
                <th>Ciudad</th>
                <th>Capacidad</th>
                <th class="text-center">Acciones</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>@Html.DisplayFor(modelItem => item.Nombre)</td>
                    <td>@Html.DisplayFor(modelItem => item.Direccion)</td>
                    <td>@Html.DisplayFor(modelItem => item.Ciudad)</td>
                    <td>@Html.DisplayFor(modelItem => item.Capacidad)</td>
                    <td class="text-center">
                        <a class="btn btn-sm btn-warning" asp-action="Edit" asp-route-id="@item.Id">
                            <i class="bi bi-pencil"></i> Editar
                        </a>
                        <a class="btn btn-sm btn-info" asp-action="Details" asp-route-id="@item.Id">
                            <i class="bi bi-eye"></i> Ver
                        </a>
                        <a class="btn btn-sm btn-danger" asp-action="Delete" asp-route-id="@item.Id">
                            <i class="bi bi-trash"></i> Eliminar
                        </a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>
